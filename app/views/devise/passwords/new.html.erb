<div class="row">
  <div class="col-sm-7">
    <div class="card">
      <div class="card-header">
        <h2>Mot de passe oublier?</h2>

        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field">
            <%= f.label :Email %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: "écrit ton email ici" %>
          </div>

          <div class="actions">
            <%= f.submit "Envoyez-moi les instructions de réinitialisation du mot de passe" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
