<div class="row">
  <div class="col-sm-5">
    <div class="card">
      <div class="card-header">
        <h1>Liste des Restaurants</h1>
    <%= link_to 'Ajoutez un restaurant', new_restaurant_path, class: 'btn btn-primary text-light' %>
    <%= link_to 'Acceuil', foods_path, class: 'btn btn-secondary text-light' %>
    <br><br>
        <table>
          <thead>
            <tr>
              <th>Nom du restaurant</th>
              <th>Adresse du restaurant</th>
              <th>Téléphone du restaurant</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @restaurants.each do |restaurant| %>
              <tr>
                <td><%= restaurant.name %></td>
                <td><%= restaurant.adress %></td>
                <td>(+229) <%= restaurant.phone %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </div>
</div>
